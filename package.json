{
  "name": "sara-techtrender",
  "version": "2.3.1",
  "description": "A study project for a university program in web development at Mid Sweden University. With focus on mashups and API integration. Powered by JavaScript, SCSS, Parcel, and Git. It features responsive design, API mashups and automated deployment. ",
  "type": "module",
  "scripts": {
    "dev": "parcel serve src/index.html --dist-dir dist",
    "build": "cross-env NODE_OPTIONS=--max_old_space_size=1024 parcel build src/index.html --no-cache --log-level verbose --no-source-maps --dist-dir dist --public-url ./ && copy _redirects dist\\_redirects || echo 'Parcel failed'",
    "clean": "rd /s /q dist && rd /s /q .parcel-cache && rd /s /q node_modules && del package-lock.json && npm cache clean --force",
    "reset": "npm run clean && npm install",
    "start": "npm run dev",
    "test": "echo \"Error: no test specified\" && exit 1",
    "deploy": "npm run build && echo '✅ Deployment files are now ready in dist/'",
    "serve": "npx serve dist -s -l 3000"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/sarasjodin/sara-techtrender.git"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "bugs": {
    "url": "https://github.com/sarasjodin/sara-techtrender/issues"
  },
  "homepage": "https://github.com/sarasjodin/sara-techtrender#readme",
  "devDependencies": {
    "@parcel/optimizer-htmlnano": "2.15.2",
    "@parcel/rust": "2.15.2",
    "@parcel/transformer-image": "^2.15.2",
    "@parcel/transformer-sass": "^2.15.2",
    "@types/node": "24.0.15",
    "buffer": "6.0.3",
    "cross-env": "^7.0.3",
    "crypto-browserify": "3.12.1",
    "events": "3.3.0",
    "os-browserify": "0.3.0",
    "parcel": "2.15.4",
    "path-browserify": "1.0.1",
    "process": "0.11.10",
    "sass": "^1.89.1",
    "stream-browserify": "3.0.0",
    "string_decoder": "1.3.0",
    "svgo": "3.3.2",
    "vm-browserify": "1.1.2"
  },
  "dependencies": {
    "dotenv": "16.5.0",
    "pbkdf2": "^3.1.3",
    "wordcloud": "1.2.3"
  }
}
