{
  "name": "graphs-and-maps-lab",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "docs": "jsdoc -c jsdoc.conf.json",
    "dev_old with doc": "parcel serve src/index.html docs/index.html src/about.html src/chart-chartistjs.html src/chart-chartsjs.html src/chart-d3js.html src/chart-highcharts.html src/map-google-api.html src/map-leaflet.html --dist-dir dist",
    "dev": "parcel serve src/index.html src/about.html --dist-dir dist",
    "parcel-build": "cross-env NODE_OPTIONS=--max_old_space_size=1024 parcel build src/index.html src/about.html --no-cache --log-level verbose --no-source-maps --dist-dir dist --public-url ./ || echo 'Parcel failed'",
    "copy-docs": "cp -r docs dist/docs || echo 'Docs folder not found, skipping'",
    "build": "npm run docs && npm run parcel-build && npm run copy-docs",
    "clean": "rd /s /q dist && rd /s /q .parcel-cache && rd /s /q node_modules && npm cache clean --force",
    "start": "npm run dev",
    "test": "echo \"Error: no test specified\" && exit 1",
    "deploy": "jsdoc src/ -r -d docs && npm run build && echo '✅ Deployment files are now ready in dist/'",
    "serve": "npx serve dist -s -l 3000",
    "docs:open": "start docs/index.html && jsdoc src/ -r -d docs",
    "docs:clean": "rd /s /q docs",
    "docs:refresh": "npm run docs:clean && jsdoc src/ -r -d docs && npm run docs && npm run docs:open"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/sarasjodin/graphs-and-maps-lab.git"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "commonjs",
  "bugs": {
    "url": "https://github.com/sarasjodin/graphs-and-maps-lab/issues"
  },
  "homepage": "https://github.com/sarasjodin/graphs-and-maps-lab#readme",
  "devDependencies": {
    "@parcel/transformer-image": "2.14.4",
    "@parcel/transformer-sass": "2.14.1",
    "cross-env": "7.0.3",
    "jsdoc": "4.0.4",
    "parcel": "2.14.4",
    "sass": "1.86.3",
    "svgo": "3.3.2"
  },
  "dependencies": {
    "chart.js": "4.4.8",
    "chartist": "1.3.0",
    "d3": "7.9.0",
    "highcharts": "12.1.2",
    "leaflet": "1.9.4"
  }
}
